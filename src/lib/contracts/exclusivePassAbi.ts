/**
 * BloomExclusivePass contract configuration.
 * ABI definitions are in escrowAbi.ts (EXCLUSIVE_PASS_ABI).
 */

// Contract address from env (set after deployment)
export const EXCLUSIVE_PASS_ADDRESS = process.env.NEXT_PUBLIC_EXCLUSIVE_PASS_ADDRESS || '';
